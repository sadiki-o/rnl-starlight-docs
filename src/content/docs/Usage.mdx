---
title: Usage
sidebar:
  label: Usage
  order: 3
---

import CustomCodePreview from "/src/components/CustomCodePreview";

# Basic Example

The simplest way to use the `ReactNavigableList` component, the list is by default scrollable :

---

<CustomCodePreview
client:load
client:only="react"
jsCode={`
  const RNL = () => {
    const [selected, setSelected] = React.useState([1]);
    const [disabled, _setDisabled] = React.useState([0, 1, 3, 9, 999]);
    const items = [];

    for (let i = 1; i <= 100; i++) {
      items.push({
        label: \`option \${i}\`,
        value: \`option\_\${i}\`,
      });
    }

    return (
        <ReactNavigableList
          items={items}
          selected={selected}
          disabled={disabled}
          multiple={false}
          checkboxOnMultiple={true}
          onChange={(selected) => {
            console.log(selected);
          }}
          className="flex h-[220px] w-[200px] flex-col justify-between border border-[#BCB8B1] bg-white px-1 py-[2px]"
        />
    );
  };
  render(<RNL/>)
`}
tsCode={`
  const RNL = () => {
    const [selected, setSelected] = React.useState<number[]>([1]);
    const [disabled, _setDisabled] = React.useState<number[]>([0, 1, 3, 9, 999]);
    const items = [];

    for (let i = 1; i <= 100; i++) {
      items.push({
        label: \`option \${i}\`,
        value: \`option\_\${i}\`,
      });
    }

    return (
        <ReactNavigableList
          items={items}
          selected={selected}
          disabled={disabled}
          multiple={false}
          checkboxOnMultiple={true}
          onChange={(selected) => {
            console.log(selected);
          }}
          className="flex h-[220px] w-[200px] flex-col justify-between border border-[#BCB8B1] bg-white px-1 py-[2px]"
        />
    );
  };
  render(<RNL/>)
`}
/>

## Non scrollable list

If you need a non scrollable list, we have got you covered :

---

<CustomCodePreview
client:load
client:only="react"
jsCode={`
  const RNL = () => {
    const [selected, setSelected] = React.useState([1]);
    const [disabled, _setDisabled] = React.useState([0, 1, 3, 9, 999]);
    const items = [];

    for (let i = 1; i <= 10; i++) {
      items.push({
        label: \`option \${i}\`,
        value: \`option\_\${i}\`,
      });
    }

    return (
        <ReactNavigableList
          items={items}
          selected={selected}
          disabled={disabled}
          multiple={false}
          checkboxOnMultiple={true}
          onChange={(selected) => {
          console.log(selected);
          }}
          overflowY={false}
          className="border border-[#BCB8B1] bg-white px-1 py-[2px]"
        />
      );
    };
    render(<RNL/>)
`}
tsCode={`
  const RNL = () => {
  const [selected, setSelected] = React.useState<number[]>([1]);
  const [disabled, _setDisabled] = React.useState<number[]>([0, 1, 3, 9, 999]);
  const items = [];

  for (let i = 1; i <= 10; i++) {
    items.push({
      label: \`option \${i}\`,
      value: \`option\_\${i}\`,
    });
  }

  return (
    <ReactNavigableList
      items={items}
      selected={selected}
      disabled={disabled}
      multiple={false}
      checkboxOnMultiple={true}
      onChange={(selected) => {
      console.log(selected);
      }}
      overflowY={false}
      className="border border-[#BCB8B1] bg-white px-1 py-[2px]"
    />
    );
  };
  render(<RNL/>)
`}
/>

## Multi Select list

For multi select cases, you can enable it using `multiple` prop, you can as well control the maximum number of selected items using `maxSelection` :

---

<CustomCodePreview
client:load
client:only="react"
jsCode={`
  const RNL = () => {
    const [selected, setSelected] = React.useState([1]);
    const [disabled, _setDisabled] = React.useState([0, 1, 3, 9, 999]);
    const items = [];

    for (let i = 1; i <= 100; i++) {
    items.push({
    label: \`option \${i}\`,
    value: \`option\_\${i}\`,
    });
    }

    return (
      <div className="flex justify-between gap-x-4 p-0 m-0">
        <div className="flex flex-col gap-y-2 !mt-0">
          <span className="">Non Scrollable</span>
          <ReactNavigableList
            items={items.slice(0, 10)}
            selected={selected}
            disabled={disabled}
            multiple
            onChange={(selected) => {
            console.log(selected);
            }}
            overflowY={false}
            className='bg-white'
        />
        </div>
        

        <div className="flex flex-col gap-y-2 !mt-0">
          <span className="">Non Scrollable</span>
          <ReactNavigableList
            items={items}
            selected={selected}
            disabled={disabled}
            multiple
            checkboxOnMultiple={true}
            onChange={(selected) => {
              console.log(selected);
            }}
            className="flex h-[240px] w-[200px] flex-col justify-between border border-[#BCB8B1] bg-white px-1 py-[2px] !mt-0"
          />
        </div>
      </div>
    );
  };
  render(<RNL/>)
`}
tsCode={`
  const RNL = () => {
    const [selected, setSelected] = React.useState<number[]>([1]);
    const [disabled, _setDisabled] = React.useState<number[]>([0, 1, 3, 9, 999]);
    const items = [];

    for (let i = 1; i <= 100; i++) {
      items.push({
        label: \`option \${i}\`,
        value: \`option\_\${i}\`,
      });
    }

    return (
      <div className="flex justify-between gap-x-4 p-0 m-0">
        <div className="flex flex-col gap-y-2 !mt-0">
          <span className="">Non Scrollable</span>
          <ReactNavigableList
            items={items.slice(0, 10)}
            selected={selected}
            disabled={disabled}
            multiple
            onChange={(selected) => {
            console.log(selected);
            }}
            overflowY={false}
            className='bg-white'
        />
        </div>
        

        <div className="flex flex-col gap-y-2 !mt-0">
          <span className="">Non Scrollable</span>
          <ReactNavigableList
            items={items}
            selected={selected}
            disabled={disabled}
            multiple
            checkboxOnMultiple={true}
            onChange={(selected) => {
              console.log(selected);
            }}
            className="flex h-[240px] w-[200px] flex-col justify-between border border-[#BCB8B1] bg-white px-1 py-[2px] !mt-0"
          />
        </div>
      </div>
    );
  };
  render(<RNL/>)
`}
/>

## Virtualized list

If you have lot of items to render say thousands, you can enable the virtualization for fast and lightweight rendering as bellow :

---

<CustomCodePreview
client:load
client:only="react"
jsCode={`
  const RNL = () => {
    const [selected, setSelected] = React.useState([1]);
    const [disabled, _setDisabled] = React.useState([0, 1, 3, 9, 999]);
    const items = [];

    for (let i = 1; i <= 100_000; i++) {
      items.push({
        label: \`option \${i}\`,
        value: \`option\_\${i}\`,
      });
    }

    return (
      <div className="flex h-[220px] w-[200px] flex-col justify-between border border-[#BCB8B1] bg-white px-1 py-1">
        <ReactNavigableList
          items={items}
          disabled={disabled}
          listItemStyles={{
            disabledClasses: 'text-pink-300',
            checkboxClasses: 'accent-black'
          }}
          multiple={true}
          onChange={selected => {
            console.log(selected);
          }}
          enableVirtualization
          overscan={14}
          windowHeight={220}
          itemHeight={20}
        />
      </div>
    );
  };
  render(<RNL/>)
`}
tsCode={`
  const RNL = () => {
    const [selected, setSelected] = React.useState<number[]>([1]);
    const [disabled, _setDisabled] = React.useState<number[]>([0, 1, 3, 9, 999]);
    const items = [];

    for (let i = 1; i <= 100_000; i++) {
      items.push({
        label: \`option \${i}\`,
        value: \`option\_\${i}\`,
      });
    }

    return (
      <div className="flex h-[220px] w-[200px] flex-col justify-between border border-[#BCB8B1] bg-white px-1 py-1">
        <ReactNavigableList
          items={items}
          disabled={disabled}
          listItemStyles={{
            disabledClasses: 'text-pink-300',
            checkboxClasses: 'accent-black'
          }}
          multiple={true}
          onChange={selected => {
            console.log(selected);
          }}
          enableVirtualization
        />
      </div>
    );
  };
  render(<RNL/>)
`}
/>
